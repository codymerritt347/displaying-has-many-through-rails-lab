<h2><%= @patient.name %></h2>
<h3>Appointments:</h3>
<ul>
<% @patient.appointments.each do |appt| %>
  <li>
  <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor)%>
  </li>
<% end %>
</ul>